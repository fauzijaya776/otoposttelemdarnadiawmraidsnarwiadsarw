# Kode lama (arsitektur userbot)

Isi folder ini adalah versi sebelumnya yang login memakai **akun Telegram pribadi**
(GramJS + panel web Express) — bukan bot BotFather.

Sudah tidak dipakai dan tidak lagi terhubung ke `src/`. Disimpan hanya sebagai arsip;
aman untuk dihapus. Dependency `telegram`, `express`, dan `multer` yang dibutuhkan
file-file ini juga sudah dilepas dari `package.json`.
